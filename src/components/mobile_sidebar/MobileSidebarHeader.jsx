import React from 'react';

const MobileSidebarHeader = ({ onLinkClick }) => {
  return (
    <div className="flex items-center justify-start mb-8 pt-2">
      {/* The logo and brand name were previously here. They have been removed as per the user's request. */}
      {/* The main navbar logo remains visible. */}
    </div>
  );
};

export default MobileSidebarHeader;